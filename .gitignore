# Machine-specific config
.machine.conf

# User-specific environment variables (contains personal info)
.env.local

# Build artifacts
*.o
*.a
*.so

# Paru cache
paru/

# Neovim build (lives in ~/personal/neovim)
